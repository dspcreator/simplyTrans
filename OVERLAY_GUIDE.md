이 버전은 Android `SYSTEM_ALERT_WINDOW`를 사용해 다른 브라우저 위에 `화면 번역하기` 플로팅 UI를 표시합니다.

사용:
1. TextBot에서 화면 공유를 누름
2. '다른 앱 위에 표시' 권한 허용
3. 화면 캡처 권한 허용
4. Chrome 등 다른 브라우저로 이동
5. 화면 위 `화면 번역 / 화면 번역하기` UI가 표시됨
6. 버튼을 누르면 현재 화면을 캡처해 기존 WebView 번역 로직으로 전달
