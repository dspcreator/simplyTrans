# SimplyTsL v7 — 다른 브라우저 위 버튼 터치

이번 버전은 오버레이 창을 `FLAG_NOT_FOCUSABLE`로 만들지 않습니다.
따라서 `화면 번역하기` 버튼이 다른 브라우저 위에서도 실제 터치 입력을 받을 수 있습니다.

동작:
1. SimplyTsL에서 화면 공유/화면 캡처 권한을 허용
2. 다른 앱 위에 표시 권한을 허용
3. Chrome 등 다른 브라우저로 이동
4. 플로팅 `화면 번역하기` 버튼을 터치
5. 버튼 터치 즉시 화면 캡처 요청
6. 번역 루프 시작

번역 버튼은 터치 중 중복 실행을 막기 위해 처리 중 잠시 비활성화되며,
화면 프레임이 전달되면 다시 활성화됩니다.
